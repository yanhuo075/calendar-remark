import { Options } from 'html-to-image/lib/types';

type LibFn = (node: HTMLElement, options?: Options) => Promise<unknown>;
type LibFnReturn<F extends LibFn> = Awaited<ReturnType<F>>;
type LibOptions = Options;

export type { LibFn, LibFnReturn, LibOptions };
